apiVersion: skaffold/v4beta6
kind: Config
requires:
  - configs: ["chicmoz-dev-base-image"]
    path: ../chicmoz-base-image.yaml
metadata:
  name: chicmoz-dev-auth-image
build:
  artifacts:
    - image: auth
      context: ../../../services/auth
      docker:
        dockerfile: Dockerfile
      requires:
        - image: chicmoz-base
          alias: BASE
