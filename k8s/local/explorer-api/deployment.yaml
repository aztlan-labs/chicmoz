apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: explorer-api
  name: explorer-api
  namespace: chicmoz
spec:
  replicas: 1
  selector:
    matchLabels:
      app: explorer-api
  strategy: {}
  template:
    metadata:
      labels:
        app: explorer-api
    spec:
      containers:
        - image: explorer-api:latest
          resources:
            limits:
              memory: 4096Mi
              cpu: 350m
          ports:
            - name: http-app
              containerPort: 80
              protocol: TCP
          name: explorer-api
          env:
            - name: NODE_ENV
              value: "development"
            - name: PORT
              value: "80"
            - name: CHAIN_NAME
              value: "AZTEC"
            - name: NETWORK_NAME
              value: "SANDBOX"
            - name: UPDATE_VALIDATOR_MAPPING
              value: "false"
            - name: BLOCK_INTERVAL_MS
              value: "2000"
            - name: BLOCK_DB_VALIDATION_ENABLED
              value: "false"
            - name: BLOCK_DB_VALIDATION_INTERVAL
              value: "50"
            - name: BLOCK_BD_VALIDATION_OFFSET
              value: "50"
            - name: CACHE_EXPIRATION_MS
              value: "5000"
          readinessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 15
            periodSeconds: 10
status: {}
