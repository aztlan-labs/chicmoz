apiVersion: v1
kind: Service
metadata:
  name: websocket-event-publisher
spec:
  selector:
    app: websocket-event-publisher
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
